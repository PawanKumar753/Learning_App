package com.app.user.repository;

import com.app.user.entity.Intrests;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IntersetsRepository extends JpaRepository<Intrests, Long> {
}
